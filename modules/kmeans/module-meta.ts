import type { LabModuleMeta } from "@/modules/lab/types";

export const kmeansModuleMeta: LabModuleMeta = {
  id: "kmeans",
  label: "K-Means",
  subtitle: "Iterative centroid clustering — watch inertia converge",
};
